fastapi==0.141.1
# starlette/anyio는 fastapi의 전이 의존성이지만 명시적으로 핀한다 — TestClient·미들웨어에
# 직접 영향을 주고, 과거에 fastapi 번프가 조용히 끌어온 starlette 변화가 pytest의
# filterwarnings=error를 깨뜨린 적이 있다(커밋 a530a38). fastapi 번프 시 함께 올린다.
starlette==1.6.0
anyio==4.15.1
uvicorn[standard]==0.53.0
sqlalchemy==2.0.54
alembic==1.20.0
psycopg2-binary==2.9.13
pydantic==2.13.5
pydantic-settings==2.15.0
python-multipart==0.0.32
pyjwt==2.14.0
# cryptography: 직접 import하진 않지만 PyJWT가 JWKS(RS256/ES256) 서명 검증에 쓴다 — 빼면 인증이 깨진다.
cryptography==50.0.1
google-auth==2.58.0
google-api-python-client==2.200.0
# app/services/google_auth.py가 google.auth.transport.requests로 직접 쓴다 — 지금은 google-api-core가
# 전이 의존성으로 끌어오지만 직접 import하는 패키지라 명시적으로 핀한다.
requests==2.34.2
httpx==0.28.1
