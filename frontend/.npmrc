# openapi-typescript(7.x, generate:api-types 스크립트 전용)가 peer로 typescript@^5.x를
# 요구하는데 이 프로젝트는 typescript 6.x를 쓴다. api-types 생성은 수동·선택 도구라
# 이 충돌만 억제한다. 다른 peer 충돌이 생기면 개별 검토할 것.
legacy-peer-deps=true
