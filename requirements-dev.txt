# 테스트/개발 전용 의존성. 프로덕션 이미지(render.yaml: requirements.txt만 설치)에는 들어가지 않는다.
# CI backend job과 로컬 개발에서만: pip install -r requirements.txt -r requirements-dev.txt
pytest==9.1.1
ruff==0.14.2
# 전체 마이그레이션 스키마 동등성 검증(scripts/verify_migration_squash.py) 전용 임베디드 Postgres.
# 평소 pytest는 SQLite라 필요 없지만, CLAUDE.md가 참조하는 검증 절차가 실제로 돌아가려면 있어야 한다.
pgserver==0.1.4
