# 테스트/개발 전용 의존성. 프로덕션 이미지(render.yaml: requirements.txt만 설치)에는 들어가지 않는다.
# CI backend job과 로컬 개발에서만: pip install -r requirements.txt -r requirements-dev.txt
pytest==9.1.1
ruff==0.16.5
# pre-commit 훅 러너 (.pre-commit-config.yaml). `pre-commit install` 로 훅 활성화.
pre-commit==4.6.2
# 임베디드 Postgres. CI의 migration-drift 잡(scripts/check_migration_drift.py)과
# 스쿼시 스키마 동등성 검증(scripts/verify_migration_squash.py)이 쓴다 — 모델·마이그레이션이
# household 스키마를 써서 SQLite로 대체할 수 없다. 평소 pytest(SQLite)에는 필요 없다.
pgserver==0.1.4
